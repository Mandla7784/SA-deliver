{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "mvn clean package && java -jar target/sa-deliver-backend.jar",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
